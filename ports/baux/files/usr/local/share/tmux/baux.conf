# Roxanne Cyberdeck tmux — Immortal + Unixporn Edition v0.2.1
# Prefix: C-Space
set -g history-limit 50000
set -g mouse on
set -g base-index 1
setw -g pane-base-index 1
set -g renumber-windows on
set -g detach-on-destroy off
set -g escape-time 0
set -g status-interval 5
set -g status on
set -g status-position top
set -g status-justify left

# Prefix
set -g prefix C-Space
unbind C-b
bind C-Space send-prefix

# Your perfect keybinds
bind -n M-h select-pane -L
bind -n M-j select-pane -D
bind -n M-k select-pane -U
bind -n M-l select-pane -R
bind -n M-H resize-pane -L 5
bind -n M-J resize-pane -D 5
bind -n M-K resize-pane -U 5
bind -n M-L resize-pane -R 5
bind -n M-% split-window -h -c "#{pane_current_path}"
bind -n M-\" split-window -v -c "#{pane_current_path}"
bind | split-window -h -c "#{pane_current_path}"
bind - split-window -v -c "#{pane_current_path}"
bind c new-window -c "#{pane_current_path}"
bind , command-prompt -I "#W" "rename-window '%%'"
bind . command-prompt -I "#S" "rename-session '%%'"
bind r source-file "/usr/local/share/tmux/baux.conf" \; display "Roxanne reloaded!"

# IMMORTAL SSH PANES
set -g remain-on-exit on
set -g remain-on-exit failed
set -g pane-border-status top
set -g pane-border-format "#[fg=#cc241d,bold] #P: #{pane_current_command} #{?pane_dead, ← DEAD →, #[fg=#98971a]LIVE#[default]} "

# TPM + resurrect + continuum (BAUX-managed for mesh consistency)
set-environment -g TMUX_PLUGIN_MANAGER_PATH '/usr/local/share/baux/tmux-plugins/'
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-resurrect'
set -g @plugin 'tmux-plugins/tmux-continuum'

# BAUX resurrect directory (system-wide, not user-specific)
set -g @resurrect-dir '/var/tmp/baux-resurrect'
set -g @resurrect-capture-pane-contents 'on'

# Essential processes for BAUX sessions
set -g @resurrect-processes 'ssh nvim btop "*baux*" baux-bot htop'

# Auto-save every 10 minutes
set -g @continuum-restore 'on'
set -g @continuum-save-interval '10'

# BAUX-specific resurrect hooks
set -g @resurrect-hook-pre-restore 'mkdir -p /var/tmp/baux-resurrect'
set -g @resurrect-hook-post-restore 'tmux display-message "BAUX sessions restored"'

run '/usr/local/share/baux/tmux-plugins/tpm/tpm'

# Roxanne status bar — Gruvbox Dark Theme
set -g status-left "#[fg=#282828,bg=#cc241d,bold] #S #[fg=#cc241d,bg=#282828]#[fg=#458588,bold]#H #[default]"
set -g status-right "#[fg=#d79921]#{?client_prefix,PREFIX ,}#[fg=#458588]%a %H:%M %Z"
set -g status-center "#[fg=#ebdbb2,bold]#{b:pane_current_path}#[default]"

# Better session name display
set -g status-left-length 30
set -g status-right-length 50

# Keyboard scrolling in copy mode (vi mode)
bind-key -T copy-mode-vi PageUp send-keys -X page-up
bind-key -T copy-mode-vi PageDown send-keys -X page-down
bind-key -T copy-mode-vi C-u send-keys -X halfpage-up
bind-key -T copy-mode-vi C-d send-keys -X halfpage-down
set -g status-style bg=#282828,fg=#ebdbb2
setw -g window-status-current-style bg=#cc241d,fg=#282828,bold

# User overrides
if-shell '[ -f /etc/baux/tmux.local.conf ]' 'source-file /etc/baux/tmux.local.conf'

# Enter respawns dead panes
bind-key Enter respawn-pane -k \; display "Reconnecting dead pane..."

# Also bind Ctrl-r for quick respawn
bind-key -n C-r respawn-pane -k \; display "Pane respawned"

